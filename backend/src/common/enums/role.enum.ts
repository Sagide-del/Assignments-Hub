// Re-exported from the generated Prisma client so every layer of the app
// (decorators, guards, DTOs, services) shares a single Role type — avoids
// TS "distinct enum" mismatches between a hand-rolled enum and Prisma's.
export { Role } from '@prisma/client';
